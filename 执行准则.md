# 执行准则

> 本文档定义 Claude Code 在前端项目中编写代码时必须遵循的流程和规范。每次编写/修改代码后，按此清单执行。

---

## 项目上下文

- **项目名**: emotion-robot (atri-webui)
- **技术栈**: Vue 3 + TypeScript + Vite + Pinia + UnoCSS
- **设计文档位置**: `D:\Coding\GitHub_Resuorse\emotion-robot\docs\`
  - `前端设计.md` — 前端架构设计的完整蓝本
  - `phase6-10-roadmap.md` — Phase 6-10 开发路线图
  - `AIRI与atri功能对比分析.md` — 功能对比和缺失分析
  - `后端API接口文档.md` — 后端 API 接口规范
  - `docs\AIRI前端功能清单.md` -- 参考项目 airi 的前端功能分析报告
- **参考项目**: `D:\Coding\GitHub_Resuorse\emotion-robot\airi` — AIRI 源码
- **实现前必读**: 修改某个模块前，先阅读对应的设计文档章节

---

## 代码编写后的检查清单

每次编写或修改代码后，按顺序执行以下步骤：

### 1. 类型检查

```bash
# 确保没有 TypeScript 类型错误
npm run type-check
# 或
npx vue-tsc --noEmit
```

### 2. 代码格式化和检查

```bash
# ESLint 检查和自动修复
npm run lint
# 或
npx eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix

# Prettier 格式化（如果配置了）
npm run format
# 或
npx prettier --write .
```

### 3. 依赖检查

- 确认新增的 import 都在 `package.json` 的依赖中
- 如果引入了新依赖：`npm install <package>`
- 区分 `dependencies`（运行时依赖）和 `devDependencies`（开发依赖）

### 4. 构建验证

```bash
# 确保构建成功
npm run build
```

- 检查构建产物大小（`dist/` 目录）
- 确保没有构建警告或错误

### 5. 设计一致性

- 检查实现是否与设计文档一致
- 如果实现中发现设计需要调整，先更新设计文档，再改代码
- 不要偏离设计文档中已确定的决策（如状态管理结构、API 对接方式等）

### 6. 本地测试验证

```bash
# 启动开发服务器
npm run dev
```

- 在浏览器中手动测试新增功能
- 检查控制台是否有错误或警告
- 验证 API 对接是否正确（需要后端服务运行）
- 验证 WebSocket 连接是否正常

### 7. 端到端测试（Phase 6 完成后）

编写test-exe.md文档，给出测试指令和期望验收成果，等待管理员验收。参考文档"D:\Coding\GitHub_Resuorse\emotion-robot\atri\tests\agent\test-exe.md"

```bash
# 1. 启动后端服务
cd D:\Coding\GitHub_Resuorse\emotion-robot\atri
uv run python -m src.main

# 2. 启动前端服务
cd D:\Coding\GitHub_Resuorse\emotion-robot\atri-webui
npm run dev

# 3. 浏览器访问 http://localhost:5173
# 4. 验证核心功能：
#    - 角色列表加载
#    - 创建新聊天
#    - 发送消息并收到流式响应
#    - 聊天历史管理
#    - 背景自定义
```

---

## 编码规范

### 命名

- **文件名**: `PascalCase.vue`（组件）/ `camelCase.ts`（工具/类型）/ `kebab-case.css`（样式）
- **组件名**: `PascalCase`（如 `ChatArea.vue`）
- **函数/变量**: `camelCase`
- **常量**: `UPPER_SNAKE_CASE`
- **类型/接口**: `PascalCase`（如 `interface ChatState`）
- **Composables**: `use` 前缀（如 `useChat.ts`）
- **Stores**: 模块名（如 `chat.ts`，导出 `useChatStore`）

### 组件结构

每个 Vue 组件遵循标准结构：

```vue
<script setup lang="ts">
// 1. imports
import { ref, computed } from 'vue'
import { useChatStore } from '@/stores/chat'

// 2. props/emits
interface Props {
  message: string
}
const props = defineProps<Props>()
const emit = defineEmits<{
  send: [text: string]
}>()

// 3. composables
const chatStore = useChatStore()

// 4. reactive state
const inputText = ref('')

// 5. computed
const isDisabled = computed(() => !inputText.value.trim())

// 6. methods
const handleSend = () => {
  emit('send', inputText.value)
  inputText.value = ''
}

// 7. lifecycle hooks
onMounted(() => {
  // ...
})
</script>

<template>
  <!-- 模板内容 -->
</template>

<style scoped>
/* 组件样式（优先使用 UnoCSS 原子类） */
</style>
```

### 目录结构

遵循设计文档`前端设计.md`中的目录结构：

```
src/
├── main.ts                      # 应用入口
├── App.vue                      # 根组件
├── router/                      # 路由配置
├── stores/                      # Pinia 状态管理
├── pages/                       # 页面组件
├── components/                  # 通用组件
│   ├── chat/                    # 聊天相关
│   ├── sidebar/                 # 侧边栏相关
│   ├── background/              # 背景相关
│   └── ui/                      # 基础 UI 组件
├── api/                         # API 接口封装
├── utils/                       # 工具函数
├── composables/                 # 组合式函数
├── types/                       # TypeScript 类型定义
└── assets/                      # 资源文件
```

### 状态管理（Pinia）

- 每个 store 独立文件，按功能模块划分
- store 命名：`use<Module>Store`（如 `useChatStore`）
- state 使用箭头函数返回初始状态
- actions 处理异步逻辑和状态更新
- getters 用于派生状态（优先使用 computed）

### API 对接

- 所有 API 调用封装在 `src/api/` 目录
- 使用 axios 实例，统一配置 baseURL 和拦截器
- API 函数返回 Promise，调用方处理错误
- 类型定义放在 `src/api/types.ts`

### 样式规范

- **优先使用 UnoCSS 原子类**（如 `class="flex items-center gap-4"`）
- 组件特定样式使用 `<style scoped>`
- 全局样式放在 `src/assets/styles/main.css`
- 避免内联样式（`style="..."`）

### TypeScript

- 所有文件使用 TypeScript（`.ts` / `.vue`）
- 为 props、emits、函数参数、返回值添加类型
- 避免使用 `any`，优先使用具体类型或 `unknown`
- 复杂类型定义放在 `src/types/` 目录

### 异步处理

- API 调用使用 `async/await`
- WebSocket 事件监听使用回调函数
- 错误处理使用 `try/catch`
- 加载状态使用 `loading` 标志

---

## 代码审查

在提交 PR 前，启动 **`/omc-code-review`** 进行自我审查：

1. **功能完整性**：是否实现了 User Story 中的所有功能点
2. **类型安全**：是否所有变量和函数都有正确的类型
3. **错误处理**：是否处理了 API 调用失败、WebSocket 断线等异常情况
4. **性能优化**：是否有不必要的重复渲染、内存泄漏
5. **用户体验**：是否有加载状态、错误提示、空状态展示
6. **代码复用**：是否有重复代码可以提取为 composable 或组件
7. **注释文档**：复杂逻辑是否有注释说明

---

## 提交规范

- 在开始 Phase 实现前（如 Phase 6），执行 `git checkout -b feat/frontend-mvp` 切换分支，在新分支上开发
- 每个 User Story 一个 commit，不要把多个不相关的改动混在一起
- commit message 格式：`<type>: <description>`
- 示例：`feat: add WebSocket manager and real-time chat functionality`
- **注意**：commit 内容应该简洁，重点描述做了什么，不附带任何的 AI 协助信息
- Note：如果遇到 GPG 签名问题，允许使用 `git commit --no-gpg-sign` 进行提交
- 在实现功能后，等待负责人验收
- 验收结束后，执行 `git push` 推送到上游仓库，并且执行 `gh pr` 提交 PR

---

## 实现顺序

按 Phase 6 执行规格文档的 User Story 顺序推进：

```
Phase 6: 前端 MVP（基础聊天界面）
  ├── US-FE-001: 项目初始化 + 基础配置
  ├── US-FE-002: 基础布局 + 路由 + 状态管理骨架
  ├── US-FE-003: API 封装 + 类型定义
  ├── US-FE-004: 角色管理 + 聊天历史
  ├── US-FE-005: WebSocket 管理器 + 实时对话
  ├── US-FE-006: 背景自定义
  └── US-FE-007: 集成测试 + 文档

Phase 7: 角色管理增强（后端 CRUD + 前端管理页面）
Phase 8: Live2D 集成（可选开关）
Phase 9: 设置页面（系统设置 + 场景设置 + 数据管理）
Phase 10: TTS/ASR 语音交互（可选）
```

## 性能优化

### 组件优化

- 使用 `v-memo` 缓存列表项
- 使用 `v-once` 渲染静态内容
- 使用 `v-show` 代替 `v-if`（频繁切换）
- 使用 `<KeepAlive>` 缓存路由组件

### 状态优化

- 避免在 computed 中执行复杂计算
- 使用 `shallowRef` / `shallowReactive` 优化大对象
- 及时清理事件监听器和定时器

### 资源优化

- 图片使用 WebP 格式
- 懒加载路由组件（`() => import('./Component.vue')`）
- 使用 CDN 加载大型依赖

---

## 禁止项

- ❌ 不要修改后端代码（前后端完全分离）
- ❌ 不要提前实现 Phase 7+ 的功能（按顺序推进）
- ❌ 不要使用 `any` 类型（除非确实无法推断）
- ❌ 不要在组件中直接调用 API（使用 composables 封装）
- ❌ 不要在 `<template>` 中写复杂逻辑（提取到 computed 或 methods）
- ❌ 不要忽略 TypeScript 类型错误（必须修复）
- ❌ 不要忽略 ESLint 警告（必须修复或合理禁用）
- ❌ 不要提交 `node_modules/` 或 `dist/` 目录
- ❌ 不要在 commit message 中包含 AI 协助信息
